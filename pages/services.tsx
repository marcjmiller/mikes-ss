import React from "react";
import Layout from "../components/Layout";

const Services = () => {
  return (
    <Layout>
      <div
        className={`services flex flex-row p-8 h-full`}
      >
        Services
      </div>
    </Layout>
  );
};

export default Services;
